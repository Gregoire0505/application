<%= simple_form_for @publication do |f| %>
  <%= f.input :title %>
  <%= f.input :author %>
  <%= select_tag(:town_id, options_for_select(@towns), :prompt => "Select the town") %>
  <%= f.input :type %>
  <%= f.input :text %>
  <%= f.button :submit %>
<% end %>
